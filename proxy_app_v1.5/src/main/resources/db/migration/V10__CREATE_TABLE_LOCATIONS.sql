CREATE TABLE IF NOT EXISTS locations
(
    id                             BIGINT             NOT NULL,
    name                           VARCHAR(1000)      NOT NULL,
    seq                            INT                NOT NULL
);
