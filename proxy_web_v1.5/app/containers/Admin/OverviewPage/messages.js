import { defineMessages } from 'react-intl';

const scope = 'com.containers.Admin.Agent';

export default defineMessages({
  requiredNotNull: {
    id: `${scope}.requiredNotNull`,
    defaultMessage: 'This field is required',
  },
});
