import { defineMessages } from 'react-intl';

const scope = 'app.components.common.QuickSearchDialog';

export default defineMessages({
  noResult: {
    id: `${scope}.noResult`,
    defaultMessage: 'Không tìm thấy kết quả',
  },
});
