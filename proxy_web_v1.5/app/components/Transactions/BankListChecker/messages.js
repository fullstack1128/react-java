import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BorrowingPage.Step1';

const messages = defineMessages({
  bankAllTitle: {
    id: `${scope}.bankAllTitle`,
    defaultMessage: 'Chọn tất cả',
  },
});

export default messages;
