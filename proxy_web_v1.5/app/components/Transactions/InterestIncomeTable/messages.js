import { defineMessages } from 'react-intl';

const scope = 'app.containers.Transactions.InterestIncomeTable';

export default defineMessages({
  endPeriodLabel: {
    id: `${scope}.endPeriodLabel`,
    defaultMessage: 'cuối kì',
  },
  suffixMonth: {
    id: `${scope}.suffixMonth`,
    defaultMessage: 'Tháng',
  },
});
